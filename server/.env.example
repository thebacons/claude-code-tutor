# Claude Code Tutor - Server Configuration

# Server port
PORT=3001

# CORS origin (frontend URL)
CORS_ORIGIN=http://localhost:5173

# Voice output directory (relative to server root)
VOICE_OUTPUT_DIR=./public/audio

# Lessons directory (relative to server root)
LESSONS_DIR=./src/tutorials/lessons

# Session timeout in milliseconds (default: 30 minutes)
SESSION_TIMEOUT_MS=1800000
